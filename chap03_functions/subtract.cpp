#include <iostream>
using namespace std;
int subtract(int, int);
 
void subtract(int a, int b) {
    Cout << "Result of Subtraction is(a>b):" << (a - b);
}
 
int main() {
    int x = 6, y = 3
            subtract(x, y);
    return 0;
}

#include <iostream>
using namespace std;
 
int main() {
    int a, b, c;
    cin >> a
    cin >> b;
    c = a - b;
    if (c > 0)
        cout << "a is the biggest";
    else if (c < 0)
        cout << "b is the biggest";
    else if (c == 0)
        cout << "Both are equal"
}
